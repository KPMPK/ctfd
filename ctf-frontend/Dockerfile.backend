FROM node:18

WORKDIR /app

COPY package.json ./
RUN npm install

#COPY . .
COPY server.js .
COPY config.py .
COPY assets/ ./assets/

# Create a directory for the database and set permissions
RUN mkdir -p /app/data && chmod 777 /app/data

CMD ["node", "server.js"]
